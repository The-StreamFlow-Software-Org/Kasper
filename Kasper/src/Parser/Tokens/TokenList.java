package Parser.Tokens;

/*

    Tokens can be in the following form:
    STATEMENT = they are not bounded by (") or (')
    DELIMITER = or (;)
    PATH = contains an arraylist of paths
    OBJECT = bounded by '(' and ')'
    LITERAL_DOUBLE = '"'
    LITERAL_SINGLE = "'"
    FUNCTION_CALL = '@'



 */